<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >
    <FrameLayout
        android:id="@+id/frameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </FrameLayout>
    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:id="@+id/rg"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:gravity="center">
        <RadioButton
            android:id="@+id/rb1"
            style="@style/radioButton1"
            android:drawableTop="@drawable/shouye_selector"
            android:checked="true"
            android:text="@string/shouye"
            />

        <RadioButton
            android:id="@+id/rb2"
            style="@style/radioButton1"
            android:drawableTop="@drawable/shipin_selector"
            android:text="@string/shipin"/>

        <RadioButton
            android:id="@+id/rb3"
            style="@style/radioButton1"
            android:drawableTop="@drawable/guanzhu_selector"
            android:text="@string/guanzhu"/>

        <RadioButton
            android:id="@+id/rb4"
            style="@style/radioButton1"
            android:drawableTop="@drawable/login_selector"
            android:text="@string/login"
            />
    </RadioGroup>

</RelativeLayout>
